nodes:
  - address: 3.15.213.6
    user: ubuntu
    role: [etcd,controlplane,worker]
    ssh_key_path: /home/dell/Ohio.pem
addon_job_timeout: 500
services:
  kubelet:
    extra_binds:
        - "/mnt:/mnt"